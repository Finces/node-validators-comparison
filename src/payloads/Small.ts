export type Small = {
    id: string;
    name: string;
    surname: string;
    height: number;
    weight: number;
    birth_date: Date;
    email: string;
    phone_number: string;
    password: string;
    repeated_password: string;
};